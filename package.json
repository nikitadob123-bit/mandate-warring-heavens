{
  "name": "mandate-warring-heavens",
  "version": "2.0.0",
  "private": true,
  "description": "MANDATE: Воюющие Небеса — original browser war RPG",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "vite --config vite.config.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check server.js && node --check public/core.js && node --check public/game.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
